<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- 关闭沙盒：必须且最优先。只保留这一个，避免和 no-container 冲突 -->
    <key>com.apple.private.security.no-sandbox</key>
    <true/>

    <!-- root 身份/切换用户 -->
    <key>com.apple.private.persona-mgmt</key>
    <true/>

    <!-- TrollStore / 自签运行基础 -->
    <key>platform-application</key>
    <true/>
    <key>get-task-allow</key>
    <true/>
    <key>dynamic-codesigning</key>
    <true/>

    <!-- 进程/调试 -->
    <key>com.apple.private.task_for_pid-allow</key>
    <true/>
    <key>com.apple.system-task-ports</key>
    <true/>
    <key>com.apple.private.cs.debugger</key>
    <true/>
    <key>com.apple.private.skip-library-validation</key>
    <true/>

    <!-- 重启 / 关机 -->
    <key>com.apple.private.system.restart</key>
    <true/>
    <key>com.apple.private.system.shutdown</key>
    <true/>

    <!-- FrontBoard 系统服务重启（iOS 内部重启机制，不需要 root） -->
    <key>com.apple.frontboard.shutdown</key>
    <true/>
    <key>com.apple.frontboard.systemapp</key>
    <true/>

    <!-- ===== IOKit 访问（Land 内核漏洞必须） ===== -->
    <!-- 允许打开 IOSurfaceRoot 等受限 IOKit UserClient -->
    <key>com.apple.private.security.iokit-user-client-class</key>
    <array>
        <string>IOSurfaceRootUserClient</string>
        <string>IOAccelSharedUserClient2</string>
        <string>IOAccelSurface2</string>
        <string>IOAccelContext2</string>
        <string>AGXDeviceUserClient</string>
        <string>AGXSharedUserClient</string>
        <string>IOGPUDeviceUserClient</string>
        <string>IOGPUSharedUserClient</string>
    </array>

    <!-- 通用 IOKit 用户客户端访问 -->
    <key>com.apple.private.iokit.user-client-access</key>
    <true/>

    <!-- 内核内存映射（Land 漏洞需要 vm_remap/vm_protect 等） -->
    <key>com.apple.developer.kernel.increased-memory-limit</key>
    <true/>

    <!-- IOKit 服务枚举权限 -->
    <key>com.apple.private.iokit.restricted-uc-class</key>
    <true/>
</dict>
</plist>
