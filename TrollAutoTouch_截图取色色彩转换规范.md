# TrollAutoTouch 截图取色 · 色彩转换规范

> 记录 createScreenIOSurface（iOS 16+ `w30r` 10-bit surface）到 8-bit 截图像素的**正确转换方法**，供以后改代码/写 Lua 找色脚本时直接调用。
>
> **结论（2026-08-27 用户实测通过）**：`w30r` 源经 **白点归一化 + 编码域对比度拉伸 `E = 1.74×C − 188.7`** 输出，截图颜色与 P3 屏屏幕观感一致（金色/蓝色/绿色不再发灰）。

---

## 1. 结论速查（直接照抄）

### 1.1 转换公式

```
1. 解析 w30r 10-bit 通道（32-bit 小端打包）:
     R = (px >> 20) & 0x3FF     G = (px >> 10) & 0x3FF     B = (px >>  0) & 0x3FF

2. 白点归一化（ERSRGB 白点 = 0.874 = 10-bit 894，894 即屏幕白）:
     v8 = v10 * (255.0 / 894.0)        → 894→255(白)  0→0  EDR 高光 clamp 255

3. 编码域对比度拉伸（复刻原版色调映射，这是防发灰的关键）:
     out = clamp( round(1.74 * v8 - 188.7), 0, 255 )
```

### 1.2 ObjC 实现（`TSScreenCapture.m` 现行代码，commit `dcc15cf`）

```objc
// fmt == 0x77333072 /* 'w30r' */  分支
uint32_t px = *(const uint32_t *)(src + x * 4);
uint16_t r10 = (px >> 20) & 0x3FF;
uint16_t g10 = (px >> 10) & 0x3FF;
uint16_t b10 = (px >>  0) & 0x3FF;

const float kEdrWhite = 894.0f;   // 10-bit ERSRGB 白点 (0x37E)
int r8 = (int)llroundf(1.74f * (r10 * (255.0f / kEdrWhite)) - 188.7f);
int g8 = (int)llroundf(1.74f * (g10 * (255.0f / kEdrWhite)) - 188.7f);
int b8 = (int)llroundf(1.74f * (b10 * (255.0f / kEdrWhite)) - 188.7f);
if (r8 < 0) r8 = 0; else if (r8 > 255) r8 = 255;   // 三个通道各自 clamp
// ... g8 / b8 同样 clamp
dst[x*4+0] = (uint8_t)r8;  dst[x*4+1] = (uint8_t)g8;  dst[x*4+2] = (uint8_t)b8;
dst[x*4+3] = 255;
```

### 1.3 适用/不适用

| 场景 | 是否走本公式 |
|---|---|
| iOS 16+ 新机（P3 屏）`createScreenIOSurface` 返回 `w30r` | **是** |
| iOS 15 老机（走 8-bit BGRA 满量程 surface） | 否，直接取 BGRA，无需转换 |
| sRGB 屏的 `w30r`（内容已按 sRGB 编码） | 按 `_screenUsesP3()` 判定，sRGB 屏无需对比度拉伸 |

---

## 2. 背景：`w30r` 源是什么

- `createScreenIOSurface`（`[UIScreen mainScreen]` 私有方法）返回绑定主屏渲染管线的全屏 IOSurface，后台/跨 App 可用。
- iOS 16+ 返回格式 `'w30r'` = `kCVPixelFormatType_30RGBLEPackedWideGamut` = **10-bit RGB101010 小端打包**。
  - bits `[31:30]` 未用，`[29:20]=R`，`[19:10]=G`，`[9:0]=B`。**R 在最高 10 位，B 在最低 10 位**（曾按反序解析导致红蓝互换，教训）。
- 该 surface 的 colorspace 是 **ERSRGB（Extended Range sRGB）**：色域 = sRGB，但编码范围扩展。
  - **SDR 白点 = 0.874（10-bit 894 = 0x37E）**，超过 894 的编码值属于 EDR/HDR 高光。
  - 所以 8-bit 输出前必须做 **白点归一化**（×255/894），否则整体偏灰暗。

---

## 3. 为什么必须做"编码域对比度拉伸"（历史路线与教训）

原版 TrollAutoScript 用 `_UICreateCGImageFromIOSurface` 由 CoreGraphics 做色彩管理，其输出（A.png）的饱和色**明显比"白点归一化直出"更浓**。先后试过 4 条路线：

| 版本 | 做法 | 结果 |
|---|---|---|
| 早期 | `w30r` 直接截断当 8-bit sRGB | 整体降饱和发灰（B.png：平均亮度 217 vs 243） |
| `c56004c` | 白点归一化 + **线性域 P3→sRGB 矩阵** | 中心白 255 正确，但彩色仍降饱和——P3 色域 > sRGB 色域，矩阵把彩色压灰，P3 屏对比屏幕仍"一层灰色遮盖" |
| `27cf421` | 白点归一化 + 8-bit 直出（不做矩阵） | 中心白 255 ✓、中性色正确，但 **C.png 手机实测饱和色（金/蓝/绿）仍明显发灰** |
| `dcc15cf`（现行） | 白点归一化 + **编码域对比度拉伸 `E = 1.74×C − 188.7`** | **用户实测正常**，与屏幕观感一致 |

**关键教训**：
1. 直出（27cf421）得到的是"白点归一化的 ERSRGB 值"，**缺少原版色调映射的饱和度增强**——显示器上会发灰。
2. 标准 P3→sRGB 矩阵也不行：它只做色域收敛，而原版输出是**饱和度增强**（中性色不变、彩色变浓），方向不同。
3. 用 **A.png（原版）与 C.png（直出）同元素样本**（金色横幅/蓝色按钮/绿色元素/灰/白）拟合，得出原版转换 = **编码域线性映射** `E = 1.74×C − 188.7`（直接对 8bit 值操作，不是在 gamma 解码后的线性域做矩阵）。此前线性域矩阵拟合在绿色样本上失败（过饱和 clamp），根源就在这里。

---

## 4. 验证数据

### 4.1 样本拟合（A.png 原版 vs 公式输出）

| 元素 | C（直出） | A（原版） | 公式 `1.74×C−188.7` |
|---|---|---|---|
| 金色横幅 (690,66) | (255,194,109) | (255,149,0) | (255,149,1) ✓ |
| 蓝色按钮 (100,1310) | (159,158,231) | (88,86,214) | (88,86,213) ✓ |
| 绿色元素 (100,600) | (160,229,177) | (89,209,120) | (90,210,119) ✓ |
| 灰 (375,200) | (247,247,250) | (242,242,247) | (241,241,246) ✓ |
| 中心白 (375,667) | (255,255,255) | (255,255,255) | (255,255,255) ✓ |

全样本误差 ≤ 2。

### 4.2 全局对比（750×1334 整图）

| 指标 | C-vs-A（直出） | **E-vs-A（对比度拉伸）** |
|---|---|---|
| 完全相同像素 | 58% | **60.1%** |
| 差异像素均值 (R,G,B) | 12.0 / 11.5 / 10.4 | **1.5 / 1.5 / 1.6** |
| 显著差异（通道差 > 30） | 4.8% | **0.2%** |

剩余差异集中在 y=0~199（顶部横幅）与 y=1200~1299（底部按钮）——那是 A/C 两次截图**内容本身不同**（动态横幅/控件状态），非转换误差。

### 4.3 用户实测

2026-08-27：安装 `dcc15cf` 版本 IPA 后手机截图确认 **颜色正常，不再发灰**。

---

## 5. 对 Lua 脚本找色的意义

- 截图输出值 = **P3 屏屏幕观感值**（浓是浓、灰是灰），Lua 脚本在 P3 屏上取色/比对**自洽**。
- 取色值可直接与屏幕上肉眼看到的颜色对应，不需要再加任何颜色修正。
- 若以后在 **sRGB 屏**（iOS 15 老机）上跑，走的是 8-bit BGRA 满量程路径，取色值同样与屏幕一致。

---

## 6. 附：完整转换流程回顾（从 IOSurface 到 Lua）

```
[UIScreen mainScreen] createScreenIOSurface
   → 10-bit w30r IOSurface (ERSRGB, 白点 894)
       ↓ 锁 surface 逐像素读 32-bit
       ↓ 位解析: R=(px>>20)&0x3FF, G=(px>>10)&0x3FF, B=px&0x3FF
       ↓ 白点归一化: v10 * 255/894
       ↓ 编码域对比度拉伸: 1.74*v8 - 188.7, clamp [0,255]
   → 8-bit RGBA 像素缓冲
       ↓ 交给 Lua: findColor / captureScreen 等
   → Lua 脚本用截图值找色，与屏幕观感一致
```
